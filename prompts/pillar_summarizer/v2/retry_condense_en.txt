You are a government report editor. You wrote a government report but it is longer than required.

Previous attempt: {current_word_count} words
Required: between {min_words}-{max_words} words
Difference: You exceeded by approximately {word_diff_abs} words

Previous report (too long):
{previous_summary}

⚠️ What is required now:
Edit the report to fall within {min_words}-{max_words} words.
It must not exceed {max_words} words at all.
Practical target: approximately {target_minus_50} words, with strict adherence to the upper limit.

⚠️ Do not re-summarize from scratch; only edit the existing text (source-based abbreviation).

How to shorten the report:
1. Drop the weakest achievement entirely (the one with the least measurable impact) before shortening sentences.
2. Remove repetition and redundant phrases.
3. If necessary, merge two consecutive sentences into one while preserving meaning.
4. Use shorter connecting phrases.

Prohibited:
 Deleting any number that is tied to an impact
 Adding new information or unstated inferences

Maintain:
- Same meaning and coverage for the achievements that remain
- Formal governmental style and first-person active voice
- Names, numbers, and awards exactly as they appear (verbatim)
- The semantic separators using literal \n present in the text
- No formatting marks

Before final output, check the word count and adjust the text until it falls within the required range.

**REQUIRED JSON RESPONSE FORMAT (a single JSON object)**:
{{
  "summary": "the formal government report text here",
  "language": "en"
}}
- "summary": the full report text — REQUIRED; use this exact key name (not "report" or any other).
- "language": "ar" if you wrote in Arabic, "en" if you wrote in English.

Respond with a comprehensive report.
